<nav class="navbar bg-primary navbar-expand-lg shadow sticky-top" style="box-shadow: 0 5px 15px rgba(0,0,0,0.08); z-index: 1030;">
    <div class="container">
        <!-- LOGO -->
        <a class="navbar-brand" href="{{ path("app_carriere_globale") }}">
            <img src="{{ asset('uploads/home/logo.png') }}" alt="RH Soft Pro" height="45">
        </a>

        <div class="d-flex align-items-center">
            <a href="{{ path("app_carriere_globale") }}" class="btn btn-light rounded-pill px-4 text-primary fw-bold">
                <i class="bi bi-briefcase-fill me-2"></i>Toutes les offres
            </a>
        </div>
    </div>
</nav>

<style>
.logo-white {
    filter: brightness(0) invert(1); /* Transforme logo noir en blanc */
}
</style>
